# LEASIR - local development environment template
# PUBLIC / LOCAL DEFAULTS ONLY.
# NEVER commit real private keys, API tokens, or credentials here.

# Blockchain (local Hardhat defaults)
RPC_URL=http://localhost:8545

# Frontend (browser-exposed public configuration)
NEXT_PUBLIC_RPC_URL=http://localhost:8545
NEXT_PUBLIC_CONTRACT_ADDRESS=